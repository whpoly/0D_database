# Core web app dependencies
Dash>=2.17,<3
crystal-toolkit>=2024.5
pymatgen>=2024.5

# Production WSGI servers (platform specific)
gunicorn>=21.2; platform_system != "Windows"
waitress>=3.0; platform_system == "Windows"
